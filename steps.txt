#1 用weiqinzou帐号先fork。
#2 在本地上用git clone克隆下来
   git clone git@github.com:weiqinzou/test_pr.git
#3 多做几次commit
#4 多做几次pr
#use commit log to close an issue
#I want to push a local branch which not exists in the remote server.
#Yes, you can just use git push pushAbranch origin to push the pushAbranch into the remote origin server.
#Next, Let's do some commits in the server, and use git fetch origin to see what will happen.
#Let's to check whether the working dir will automatically change to keep consistent with the remote branch.

use a new clone to test the machnism of pull and fetch
